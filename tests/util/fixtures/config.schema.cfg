# test temp environment config
[test_wc_utils]
    [[sec_1]]
        name = string(default=None)
        # a string

        integer = integer(default=8)
        # an integer

        float = float(default=0.0)
        # a float
